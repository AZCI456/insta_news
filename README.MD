1. reverse tunnel setup orignally to run through mac, however managing iot in clamshell mode (the automation) was difficult so pivotted to a cheap res proxy
2. gemini setup - batched multiple captions for processing so could continue on free dev teir rather than using api credits
3. rsyc setup for dev on mac sync with server setup as git-push-pull was wastivveand vs-code remote-ssh was too much memory for the droplet (currently on lowest tier)

