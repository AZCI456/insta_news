"""
Top-level package for code living under the `src/` directory.

Teaching note:
- This file marks `src` as a Python package so imports like `import src.web`
  work when the repository root is on `PYTHONPATH` (e.g. `pip install -e .`
  or running from the project root with `python -m src.web.main`).
"""
