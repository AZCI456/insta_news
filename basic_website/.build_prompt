"Create a FastAPI app in main.py. It should:

Use Jinja2 templates to serve a single HTML file at the root /.

Have a POST /signup endpoint that takes an email and a list of club_ids.

Generate a UUID for the manage_token and save the user.

If the email already exists, update their club_ids instead of creating a new user."

Prompt 3: The Frontend (The "v0" Alternative)

"Generate a single index.html file using Tailwind CSS via CDN. Make it look like a high-end academic dashboard. It should feature:

A clean header 'UniMelb Club Oracle'.

A simple input for 'Student Email'.

A multi-select or grid of checkboxes for clubs (fetch the list from my accounts table).

Use standard Fetch API to send data to the /signup endpoint and show a 'Check your email' success message."

do this in basic_website and where possible tell and teach me in the comments. I know some beautiful soup and teh fundamentals of css and html